# How to self host


### install dependencies
> npm i
### create .env file in the root folder
> replace VITE_WS_URL=ws://localhost:8080 with your ip/domain:8080
### run the client and the server
> npm run dev
> npm run 

